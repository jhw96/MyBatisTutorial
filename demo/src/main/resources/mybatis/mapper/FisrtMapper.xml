<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN"
				"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.first.dao.FirstDAO">
	<select id="listBook" resultType="com.example.demo.first.dto.Book">
		SELECT 
			   title,
			   author,
			   detail
		FROM test.book
	</select>

</mapper>